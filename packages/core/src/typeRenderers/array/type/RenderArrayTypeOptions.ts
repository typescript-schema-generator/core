import { type RenderArrayTypeStrategy } from './RenderArrayTypeStrategy';

/**
 * Options for rendering an array type.
 */
export interface RenderArrayTypeOptions {
    strategy: RenderArrayTypeStrategy;
}
